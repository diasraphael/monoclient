# Force resolution to use registry
registry=https://registry.npmjs.org/
# Ensure strict peer dependencies
strict-peer-dependencies=false
# Disable hoisting for better compatibility
shamefully-hoist=false
