{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm build --filter=good-shepherd",
  "installCommand": "pnpm install --frozen-lockfile",
  "devCommand": "pnpm dev",
  "outputDirectory": ".next"
}
